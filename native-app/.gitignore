# Local secrets — never commit
.env
.env.local

# Node.js artifacts — installed in CI per build
node_modules/

# Android build outputs — produced by the workflow, copied to crm/dist/
android/build/
android/.gradle/
android/local.properties
android/app/build/
android/app/release/
android/captures/
android/.idea/
android/*.iml
android/app/*.iml

# Capacitor sync output (generated)
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json

# OS
.DS_Store
Thumbs.db
